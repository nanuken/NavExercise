(function (huge) {
    huge.events = huge.events || {};

    /**
     * UI events list
     */
    huge.events = {
        RESET_MENU: "RESET_MENU",
        SHOW_OVERLAY: "showOverlay",
        HIDE_OVERLAY: "hideOverlay",
    };

}(window.huge));